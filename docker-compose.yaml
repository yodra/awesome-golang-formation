version: '3.8'

services:
  mysql:
    image: mysql:8.0.22
    ports:
      - 3306:3306
    volumes:
      - mysql_data:/var/lib/mysql
      - ./sql:/docker-entrypoint-initdb.d
    environment:
      MYSQL_DATABASE: leanmind
      MYSQL_USER: leanmind
      MYSQL_PASSWORD: leanmind
      MYSQL_ROOT_PASSWORD: leanmind

volumes:
  mysql_data: